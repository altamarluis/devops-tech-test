# DevOps Automation & AI Agent Test

This repository contains a minimal Python application used to demonstrate
CI/CD automation, semantic versioning, multi-environment deployments, and
a conceptual AI agent for bug resolution.

## Run locally
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
